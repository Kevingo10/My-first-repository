# Voy a unir dos listas y cambiar el orden de las palabras

first_list = ['Hay, ', 'en ', 'que, ', 'iteracion ', 'indice ', 'muy ']
second_list = ['casos', 'los', 'la', 'por', 'es', 'util']
for i in range(len(first_list)):
    print(first_list[i] + second_list[i])

