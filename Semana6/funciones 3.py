# Cree una función que retorne la suma de todos los números de una lista.
# La función va a tener un parámetro (la lista) y retornar un numero (la suma de todos sus elementos).
# ejms: [4, 6, 2, 29] → 41


def suma(lista):
    return sum(lista)


resultado = suma([50, 50, 100])
print(resultado)